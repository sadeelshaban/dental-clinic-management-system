# Public API origin consumed by the Vite app (no trailing slash).
# Local development against docker-compose / dotnet run:
VITE_API_BASE_URL=http://localhost:5062

# Docker/nginx same-origin (leave empty so /api is proxied by nginx):
# VITE_API_BASE_URL=
